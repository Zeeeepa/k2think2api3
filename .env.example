# K2Think API Proxy Configuration Example
# Copy this to .env and update with your values

# Required: API key for client authentication
VALID_API_KEY=sk-k2think-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Required: File containing K2Think JWT tokens (one per line)
TOKENS_FILE=data/tokens.txt

# Optional: K2 credentials file for token auto-update
ACCOUNTS_FILE=data/accounts.txt

# Optional: Server configuration
HOST=0.0.0.0
PORT=7001

# Optional: Token auto-update settings
ENABLE_TOKEN_AUTO_UPDATE=true
TOKEN_UPDATE_INTERVAL=3600

# Optional: Logging
LOG_LEVEL=INFO
LOG_FILE=server.log